<div class="col-sm-8">
    <ul class="nav nav-tabs nav-justified mb-3">
      <li class="nav-item"><a href="<%= user_path(@user) %>" class="nav-link <%= 'active' if current_page?(user_path(@user)) %>">投稿一覧  <span class="badge badge-secondary"><%= @count_posts %></span></a></li></a></li>
      <li class="nav-item"><a href="<%= favorites_user_path(@user) %>" class="nav-link <%= 'active' if current_page?(favorites_user_path(@user)) %>">お気に入り一覧  <span class="badge badge-secondary"><%=  @count_favorites %></span></a></li>
    </ul>
    <%= render 'posts/posts', posts: @favorites %>
</div>