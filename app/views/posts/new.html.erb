<h1>投稿</h1>

<div class="col-sm-6 offset-sm-3">
  <div>
    <%= form_for @post do |f| %>
    <div class="form_group">
      <%= f.label :url, 'URL' %>
      <%= f.url_field :url, class: 'form_contorol', :size => "69" %>
    </div>
    
    <div class="form-group">
      <%= f.text_area :content, class: 'form-control', :size => "100x7"  %>
    </div>
    <div>
      <%= f.file_field :img, class: "image" %>
      <p>※画像をアップロードする場合はこちらからファイルを選択してください</p>
    </div>
    <div class="text-center">
      <%= f.submit '投稿する', class: 'btn btn-secondary rounded-pill' %>
      <% end %>
    </div>
  </div>
</div>
    